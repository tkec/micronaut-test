micronaut:
    application:
        name: micronaut-hello-world
    server:
        port: 8080

eureka:
    client:
        registration:
            enabled: true
        defaultZone: "${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}"
